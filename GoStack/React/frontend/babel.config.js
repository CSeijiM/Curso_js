module.exports = {
    presets: [
        '@babel/preset-env', //entende o ambiente que esta, e faz a conversão com base no ambiente.
        '@babel/preset-react' //adiciona a funcionalidades do react na conversão
    ],
};